# Syntax Briefing

The website is generated from **`index.md`**.

To edit the site, open `index.md` on GitHub, click the pencil icon, make your Markdown changes, and commit them. GitHub Pages will rebuild the website automatically.

You normally do not need to edit `_layouts/default.html`, `_config.yml`, or `.github/workflows/pages.yml`.
